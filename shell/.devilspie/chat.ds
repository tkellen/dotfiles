(if
  (contains (application_name) "Slack")
  (begin
    (set_workspace 5)
    (maximize)
    (geometry "3840x1080+0+0")
  )
)
(if
  (contains (application_name) "Skype")
  (begin
    (set_workspace 4)
    (maximize)
  )
)
